struct {

    int a;
    float b;
    string c;
     struct {
        int p;
        float q;
    } nestedStruct;
} miEstructura;

func int main() {
    int x, resultado;
   

    // Inicialización de miEstructura
    miEstructura.r = 10;
    miEstructura.b = 3.14;
    miEstructura.c = "Hola Mundo";

    // Inicialización de nestedStruct
    miEstructura.nestedStruct = 5;
    

    // Operación usando un campo de miEstructura
    x = miEstructura.a;

    print("Valor de x:");
    print(x);

    print("Valor de miEstructura.b:");
    print(miEstructura.b);


    return 0;
}

